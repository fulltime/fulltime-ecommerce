import { Jost, Poppins } from 'next/font/google'

export const jost = Jost({ subsets: ['latin'], display: 'swap' })

export const poppins = Poppins({ subsets: ['latin'], weight: ["400"], display: 'swap' })